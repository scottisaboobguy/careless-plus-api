services:
  - type: web
    name: careless-plus-api
    env: node
    plan: free
    buildCommand: npm install && pip3 install -U yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
